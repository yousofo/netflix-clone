import Nav from './Nav/nav'

const Error = () => (
  <Nav>
    <main className='md:pt-28 text-center'>
      <p className='absolute centered'>URL not found :{"("}</p>
    </main>
  </Nav>
)

export default Error